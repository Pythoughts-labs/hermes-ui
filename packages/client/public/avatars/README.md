# Preset avatars

Drop PNG/JPEG/WebP images here to add them to the preset avatar gallery.
Files are served at `/avatars/<filename>` (e.g. `/avatars/fox.png`) by Vite
without a rebuild.

Recommended: square images, 256×256 or 512×512, < 500 KB.
</content>
</invoke>